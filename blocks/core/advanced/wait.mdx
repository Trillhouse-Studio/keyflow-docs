---
title: Wait
---

## Overview

The `Wait` block is designed to pause the execution of a workflow for a specified duration. This block is useful when you need to wait for a specific amount of time before proceeding with the next steps in your workflow.
